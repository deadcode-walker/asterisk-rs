name: Semver

on:
  pull_request:
    branches: [main]

jobs:
  semver:
    name: Semver Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: obi1kenobi/cargo-semver-checks-action@v2
